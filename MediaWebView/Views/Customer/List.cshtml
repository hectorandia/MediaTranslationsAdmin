@using MediaAdmin.MediaEntity
@model IEnumerable<Customers>

@{
    ViewBag.Title = "List";
}

@foreach(var p in Model)
{
    <div>
        <h3>@p.Name</h3>
        <h4>@p.CustomerID</h4>
    </div>
}

<h2>List</h2>

